{
  "name": "@turntabl-score-room/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "build": "wrangler deploy --dry-run --env production --outdir dist",
    "deploy": "wrangler deploy --env production",
    "sync:rooms": "bun src/scripts/sync-rooms.ts",
    "sync:live-scores": "bun src/scripts/sync-live-scores.ts",
    "load:test": "bun src/scripts/load-test.ts",
    "load:cleanup": "bun src/scripts/cleanup-load-test.ts",
    "types": "wrangler types src/worker-configuration.d.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.108.2",
    "@turntabl-score-room/shared": "workspace:*",
    "hono": "^4.9.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260617.1",
    "bun-types": "^1.3.13",
    "typescript": "^5.8.3",
    "wrangler": "^4.101.0"
  }
}
